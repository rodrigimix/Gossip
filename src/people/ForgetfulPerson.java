package people;

/**
 * @author Rodrigo Marques n_64411 && David Pinto n_64609
 *
 * The interface of the ForgetfulPersonClass class.
 * It contains method that is unique to this type of Person.
 */
public interface ForgetfulPerson {

    /**
     * Resets the current used to move through positions of array of gossips.
     */
    void defaultCurrent();
}
